import { LoginForm } from "@/components/login-form"

export default function LoginPage() {
  return (
    <main className="min-h-[100dvh] grid place-items-center p-4 bg-muted">
      <LoginForm />
    </main>
  )
}
